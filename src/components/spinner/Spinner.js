import spinner from './spinner.svg'
import './spinner.scss';

const Spinner = () => {
    
    return (
        <>
            <img src={spinner} alt="" className="spinner"/>
        </>
    )
}

export default Spinner;